services:
  postgres:
    image: 'postgres:16'
    environment:
      POSTGRES_DB: wealthdb
      POSTGRES_USER: wealth_user
      POSTGRES_PASSWORD: wealth_pass
    ports:
      - '5432:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data

volumes:
  postgres-data:
