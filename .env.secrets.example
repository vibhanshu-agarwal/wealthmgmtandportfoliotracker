# Terraform Backend (Bootstrap)
TF_STATE_BUCKET=vibhanshu-tf-state-2026
TF_LOCK_TABLE=vibhanshu-terraform-locks

# App Infrastructure Secrets
POSTGRES_CONNECTION_STRING=
MONGODB_CONNECTION_STRING=
AUTH_JWK_URI=
CLOUDFRONT_ORIGIN_SECRET=

# RDS (required by Terraform plan/apply — GitHub secret names must match)
RDS_MASTER_USERNAME=
RDS_MASTER_PASSWORD=

# AWS Credentials (if not using OIDC)
AWS_ACCESS_KEY_ID=
AWS_SECRET_ACCESS_KEY=

# ---------------------------------------------------------------------------
# Api-gateway downstream (PORTFOLIO / MARKET_DATA / INSIGHT)
# ---------------------------------------------------------------------------
# GitHub Actions / AWS Lambda: use each service’s HTTPS Lambda Function URL, e.g.
#   https://xxxxxxxx.lambda-url.<region>.on.aws/
# (from `terraform output` or Lambda → Configuration → Function URL.)
#
# Local “modular monolith” / dummy routes (single stub on one port), e.g. before go-live:
#   PORTFOLIO_SERVICE_URL=http://localhost:8080
#   MARKET_DATA_SERVICE_URL=http://localhost:8080
#   INSIGHT_SERVICE_URL=http://localhost:8080
#
# Local split services on host default ports:
#   PORTFOLIO_SERVICE_URL=http://localhost:8081
#   MARKET_DATA_SERVICE_URL=http://localhost:8082
#   INSIGHT_SERVICE_URL=http://localhost:8083
#
# Api-gateway inside Docker Compose: use Compose service hostnames (not localhost), e.g.
#   http://portfolio-service:8081 — see docker-compose.yml for the gateway service env.
#
# WARNING: ./scripts/sync-secrets.sh .env.secrets runs `gh secret set -f` and uploads every
# KEY=value in the file to the GitHub repo. Do not sync localhost dummy URLs to a production
# repository; replace with real Function URLs (and other prod secrets) before syncing for prod.
PORTFOLIO_SERVICE_URL=
MARKET_DATA_SERVICE_URL=
INSIGHT_SERVICE_URL=
