# 🛑 Architectural Review & Corrections

You executed the steps without waiting for confirmation and missed critical architectural components. We need to fix the CDK code immediately.

Please update the CDK stacks to address the following three missing requirements:

### 1. Fix Missing Service Discovery (ECS Cloud Map)
Currently, our `api-gateway` cannot route to the backend services because there is no DNS resolution in the ECS cluster.
* In `compute-stack.ts`, update the ECS Cluster to include a `defaultCloudMapNamespace` (e.g., `name: 'wealthmgmt.local'`).
* Update the `portfolioService` and `marketDataService` Fargate definitions to include `cloudMapOptions` (e.g., `name: 'portfolio-service'`).

### 2. Fix Missing Kafka Broker (Data Layer)
You forgot to provision Kafka for our event-driven architecture.
* In `database-stack.ts`, define an **Amazon MSK Serverless** cluster (or a simple single-node managed Kafka construct if Serverless is unavailable in the standard library).
* Export the Kafka Bootstrap Servers string.
* In `compute-stack.ts`, inject the `KAFKA_BOOTSTRAP_SERVERS` environment variable into both the `portfolioService` and `marketDataService` task definitions so they can communicate.

### 3. Fix the Empty Entrypoint (Stack Wiring)
Your `infrastructure-stack.ts` is empty.
* Delete `infrastructure-stack.ts`.
* Open the actual CDK entrypoint file (usually `bin/infrastructure.ts` or whatever `bin/*.ts` file was generated by `cdk init`).
* Instantiate `NetworkStack`, `DatabaseStack`, and `ComputeStack` in that file. Pass the VPC from the Network stack into the Database and Compute stacks. Pass the database/redis/kafka outputs from the Database stack into the Compute stack.

**Output the updated files and wait for my review. DO NOT run `cdk deploy`.**