# Spec A 9.14 Reopen-Ingress — Senior Review Orientation

> **For the reviewer:** this asks for a **plan-acceptance decision only**. Terraform apply,
> `production` Environment approval, and the live 9.14 checkpoint remain owner-controlled and are
> **not** authorized by this note or by accepting it.

## Run identity

| Field | Value |
| --- | --- |
| Run | [Terraform Azure Infrastructure #33313072724](https://github.com/vibhanshu-agarwal/wealthmgmtandportfoliotracker/actions/runs/33313072724) |
| Pinned `main` SHA | `66bbee0bf438706146ac9975bf5f0c923b3d43cb` (PR #184, "Spec A 9.14: guarded ingress reopen/close profiles") |
| Profile | `spec-a-9.14-reopen-ingress` |
| Action | `remote-plan` — read-only. Apply skipped. |
| Guard | [`assert_spec_a_9_14_plan.py`](../../../infrastructure/terraform/azure/scripts/assert_spec_a_9_14_plan.py) |

## Plan result

`0 to add, 1 to change, 0 to destroy`

The sole proposed change is an in-place update to `module.api_gateway.azurerm_container_app.this`,
adding the canonical API gateway ingress configuration: external ingress enabled, target port
`8080`, transport `auto`, `allow_insecure_connections=false`, and a single traffic split of 100% to
the latest revision.

## Acceptance contract

This is the contract you are being asked to certify. It has three tiers; each item is stated as a
condition, not as a narrative claim, so a reject can name an id.

### A. Preconditions — must hold for the plan to be meaningful

| Id | Condition | Status |
| --- | --- | --- |
| A1 | 9.14's Go condition — checkpoints 9.9 through 9.13 all green | Satisfied. 9.13 is live green on `portfolio-service--0000092`, `market-data-service--0000079`, `insight-service--0000079` (runs [33306477527](https://github.com/vibhanshu-agarwal/wealthmgmtandportfoliotracker/actions/runs/33306477527) / [33306874697](https://github.com/vibhanshu-agarwal/wealthmgmtandportfoliotracker/actions/runs/33306874697)) |
| A2 | Dispatched from the immutable pinned SHA, equal to `origin/main` at dispatch | Satisfied |
| A3 | All four supplied `SERVICE_VERSION` tags independently resolved in ACR before planning | Satisfied |
| A4 | Starting live state — gateway ingress closed, gateway on `api-gateway--0000077` | Satisfied |

### B. Plan-scope invariants — what the guard actually certifies

| Id | Invariant |
| --- | --- |
| B1 | Exactly one non-no-op resource change, and its address is `module.api_gateway.azurerm_container_app.this` |
| B2 | Its action is an in-place update — no create, replace, or destroy anywhere in the plan |
| B3 | `before` has ingress absent/disabled; `after` has exactly one ingress block whose non-default operator-controlled keys are exactly `{external_enabled, target_port, transport, traffic_weight}`, with `external_enabled=true`, `target_port=8080`, `transport=auto`, and a single `{percentage: 100, latest_revision: true}` weight |
| B4 | Gateway `min_replicas` is `0`-or-unset on **both** sides — this plan does not scale the gateway up |
| B5 | Gateway image is byte-for-byte unchanged |
| B6 | Gateway carries exactly one `SERVICE_VERSION` env entry, unchanged, with no duplicate or malformed env entries |
| B7 | No gateway field other than `ingress` changes |

**One mechanism nuance worth your attention.** The summary claim "all four `SERVICE_VERSION` values
unchanged" is carried by two different checks: B6 asserts it directly on the gateway, while the
other three services are covered only because B1's exact-scope rule forces them to appear as
no-ops. The same is true of the replica-count, catalog-enforcement, market-data Job, seed-image, and
Job-recreation claims — they are scope-derived, not per-resource diffs. `allow_insecure_connections=false`
is likewise enforced indirectly: it is stripped as a provider default, so a `true` value would leave
an extra operator key and fail B3's set-equality. If you want any of these asserted directly instead
of derived, say so — that is a guard change, and in my view a follow-up rather than an apply blocker,
but the call is yours.

### C. Live-state boundary

| Id | Condition |
| --- | --- |
| C1 | Read-only remote plan; Terraform apply skipped |
| C2 | Post-run production read confirms ingress still closed and gateway still on `api-gateway--0000077` |
| C3 | No deployment, ingress change, flag change, or other production write occurred |

## What acceptance does and does not mean

**Accepting means:** the proposed one-resource, ingress-only in-place update is correctly scoped and
is an acceptable plan for a *separately authorized* `spec-a-9.14-reopen-ingress` apply.

**Accepting does not mean:** authorizing that apply, approving the `production` Environment, marking
checkpoint 9.14 complete, or authorizing any follow-on work. Those remain owner-controlled and
require their own explicit authorization tied to a then-current SHA.

## Reversal path

A guarded reverse profile `spec-a-9.14-close-ingress` exists and is asserted by the same script in
the opposite ingress direction, so a reopen is reversible by the same bounded mechanism.

Independent of that, the standing 9.14 rollback rule from `tasks.md` applies from this checkpoint
onward: disabling the holding validator, **or** rolling back to an R3 artifact whose defaults are
permissive, requires quiescing writes first and keeping them closed until the forward fix deploys.

## Decision requested

Please return one of:

- **ACCEPT** — the plan satisfies A1–A4, B1–B7, and C1–C3, and is acceptable for a separately
  authorized `spec-a-9.14-reopen-ingress` apply.
- **ACCEPT WITH CONDITIONS** — name each condition and state explicitly whether it blocks apply or
  is follow-up work.
- **REJECT** — name the failing id(s) and the correction required before re-plan.

Please cite the ids you relied on, so a later reader can tell which invariants were actually
exercised by the review.
